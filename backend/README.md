# FixLocal backend

See the main project README: [../README.md](../README.md).

```bash
cp .env.example .env
npm install
npm run seed
npm run dev
```

Seed password: `Password123!` (or `SEED_PASSWORD` in `.env`).
